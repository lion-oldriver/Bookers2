<h1>Editing Book</h1>
<%= form_with model:@book,local:true do |f| %>
  <label>Name</label>
  <%= f.text_field :title %>
  <label>Opinion</label>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
<% end %>(